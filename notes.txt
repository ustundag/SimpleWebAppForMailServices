1-Run this command under 'mailmodule' folder;
	$ npm install (installs dependencies listed in package.json)

2-Run "$ node server.js" command to start web server of express.js framework and see example rest responses on 
	http://localhost:3000
